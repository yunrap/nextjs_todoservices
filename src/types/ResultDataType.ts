export type CheckBoxProps = {
  id: string;
  isCompleted: boolean;
  name: string;
};

export type DetailProps = {
  id: string;
  imageUrl: string;
  isCompleted: boolean;
  memo: string;
  name: string;
  tenantId: string;
};
